@require '../../vars'

.z-positioned-overlay
  position: absolute
  width: 100%
  height: 100%
  bottom: 0
  left: 0
  pointer-events: none

  > .content
    position: absolute
    pointer-events: all

    &.is-mounted
      transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1)

  > .backdrop
    position: fixed
    pointer-events: all
    top: 0
    left: 0
    width: 100%
    height: 100%
    background: getColor('$bgText38')
    -webkit-tap-highlight-color: getColor('$transparent')
    opacity: 0
    transition: opacity 0.2s cubic-bezier(0.4, 0, 0.2, 1)
