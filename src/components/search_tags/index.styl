@require '../../vars'

.z-search-tags
  flex1()
  padding: 20px 20px 12px 24px
  transition: box-shadow 0.25s

  &:hover
    shadow-1()
    cursor: pointer

  > .title
    font-tbd-name()

  > .tags
    font-body1(medium)
    margin-top: 8px

  @media (min-width: $breakPointMedium)
    > .tags
      margin-top: 4px

